import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  navItemDisabled: {
    opacity: 0.4,
  },
  container: {
    flex: 1,
    marginTop: 50,
  },
});

export default styles;
