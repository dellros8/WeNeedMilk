import { StyleSheet } from 'react-native';
import { DARK_GREY } from '../../misc/variables';

const styles = StyleSheet.create({
  text: {
    padding: 20,
    fontSize: 16,
    fontStyle: 'italic',
    color: DARK_GREY,
  },
});

export default styles;
